(async()=>{const store=window.SuraeStore;if(!store)return;await store.ready;const money=n=>Number(n||0).toLocaleString('en-PH',{minimumFractionDigits:2,maximumFractionDigits:2});const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function logout(){store.logout();location.href='index.html'}function widget(user){const w=document.createElement('div');w.className='store-player-session';w.innerHTML=`<a class="store-player-info" href="player.html"><span class="store-player-avatar">${esc((user.name||'P').charAt(0).toUpperCase())}</span><span class="store-player-copy"><strong>${esc(user.name||'Player')}</strong><small><b>₱${money(user.credits)}</b> Surae Credits</small></span></a><button class="store-player-logout" type="button">LOG OUT</button>`;w.querySelector('button').onclick=logout;return w}function render(){const user=store.getCurrentUser();const targets=[document.querySelector('.nav-actions'),document.querySelector('.ultimate-nav-actions, .nav-cta-wrap')].filter(Boolean);document.querySelectorAll('.store-player-session').forEach(x=>x.remove());if(!user){document.body.classList.remove('player-logged-in');return}for(const t of targets){if(!t.querySelector('.store-player-session'))t.appendChild(widget(user))}document.body.classList.add('player-logged-in')}render();window.addEventListener('surae-account-ready',render)})();
